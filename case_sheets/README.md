# Case Sheets

The files in this directory are small source-grounded case sheets.
The `examples/` files are hand-authored workflow and evidence summaries.
The repository also includes a local PGLib-UC parser for building a case sheet from a local JSON file.
These files are not solver inputs and they are not deployment records.
